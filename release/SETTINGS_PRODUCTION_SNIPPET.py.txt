# =========================
# SmartGeoAI v1.0 Production Security
# =========================

import os

DEBUG = os.getenv("DEBUG", "False").lower() == "true"

ALLOWED_HOSTS = [
    "fastappeal.uz",
    "www.fastappeal.uz",
]

CSRF_TRUSTED_ORIGINS = [
    "https://fastappeal.uz",
    "https://www.fastappeal.uz",
]

# Reverse proxy (Nginx) HTTPS ishlatsa:
SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")

# Productionda HTTPS yoqilgandan keyin:
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True

# Browser security
SECURE_CONTENT_TYPE_NOSNIFF = True
X_FRAME_OPTIONS = "DENY"
SECURE_REFERRER_POLICY = "same-origin"

# HTTPS to'liq ishlayotganidan keyin yoqish:
SECURE_SSL_REDIRECT = True

# HSTS ni avval kichik qiymat bilan sinang.
# Hammasi to'g'ri ishlagach 31536000 ga oshiriladi.
SECURE_HSTS_SECONDS = 3600
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = False
